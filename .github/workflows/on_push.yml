name: docker-compose-actions-workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack, test and liting
        run: docker-compose run --rm app sh -c "python manage.py test && flake8"